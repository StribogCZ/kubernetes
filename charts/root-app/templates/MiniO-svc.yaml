apiVersion: v1
kind: Service
metadata:
  name: MiniO-Node-Port
spec:
  selector:
    app.kubernetes.io/name: minio
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
    - protocol: TCP
      port: 9090
      targetPort: 9090

